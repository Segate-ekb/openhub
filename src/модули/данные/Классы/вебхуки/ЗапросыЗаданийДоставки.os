// Запросы к сущности «ЗаданиеДоставки»: только деривуемые autumn-data методы.
// Доменный API — ХранилищеЗаданийДоставки.

// BSLLS:UnusedParameters-off

&Родитель
Перем ХранилищеСущностей;

// Вся очередь без отбора. Отбор по НеРанее не деривуется намеренно: на КоннекторJSON
// запрос с отбором по ещё не созданной таблице падает, а выборка без отбора отдаёт пусто.
//
// Возвращаемое значение:
//  Массив из ЗаданиеДоставки.
//
&МетодЗапроса
Функция ПолучитьПо() Экспорт
	Возврат Неопределено;
КонецФункции

&ХранилищеСущностей("ЗаданиеДоставки")
Процедура ПриСозданииОбъекта()
КонецПроцедуры
