// Чтение того, что прислал клиент: тело запроса, его поля и заголовки.

Перем Ответы;        // ОтветыAPI (auth) — разбор тела и чтение поля
Перем Заголовки;     // ЗаголовкиHTTP (основа)
Перем Флаги;         // ПриведениеФлагов (основа) — терпимое чтение булева

// Разбирает тело запроса в поля.
//
// Параметры:
//  ТелоЗапроса - Строка - тело запроса (application/json либо form-urlencoded).
//  ЗаголовкиЗапроса - Соответствие - заголовки запроса.
//
// Возвращаемое значение:
//  Соответствие - поля тела.
//
Функция РазобратьТело(Знач ТелоЗапроса, Знач ЗаголовкиЗапроса) Экспорт
	Возврат Ответы.РазобратьТело(ТелоЗапроса, ЗаголовкиЗапроса);
КонецФункции

// Строковое значение поля тела.
//
// Параметры:
//  Тело - Соответствие - разобранное тело запроса.
//  Имя - Строка - имя поля.
//  ПоУмолчанию - Строка - что вернуть, если поля нет (необязательный,
//                по умолчанию пустая строка).
//
// Возвращаемое значение:
//  Строка - значение поля либо значение по умолчанию.
//
Функция Поле(Знач Тело, Знач Имя, Знач ПоУмолчанию = "") Экспорт
	Возврат Ответы.Поле(Тело, Имя, ПоУмолчанию);
КонецФункции

// Булево из значения поля тела: JSON отдаёт настоящий Булево, form-urlencoded — строку.
//
// Параметры:
//  Значение - Произвольный - сырое значение поля.
//
// Возвращаемое значение:
//  Булево.
//
Функция ВБулево(Знач Значение) Экспорт
	Возврат Флаги.ФлагИстина(Значение, Ложь);
КонецФункции

// Просит ли тело снять токен доступа с источника. Пустое поле token означает
// «оставить прежний», поэтому возврат к анонимному чтению просят отдельным флагом.
//
// Параметры:
//  Тело - Соответствие - разобранное тело запроса.
//
// Возвращаемое значение:
//  Булево.
//
Функция СнятиеТокенаИсточника(Знач Тело) Экспорт

	Для Каждого Имя Из СтрРазделить("token_remove,снять_токен", ",") Цикл
		Если Тело.Получить(Имя) <> Неопределено Тогда
			Возврат ВБулево(Тело.Получить(Имя));
		КонецЕсли;
	КонецЦикла;
	Возврат Ложь;

КонецФункции

// Значение заголовка запроса по имени; регистр имени не значим.
//
// Параметры:
//  ЗаголовкиЗапроса - Соответствие, Неопределено - заголовки запроса.
//  Имя - Строка - имя заголовка.
//
// Возвращаемое значение:
//  Произвольный, Неопределено - значение заголовка либо Неопределено, если его нет.
//
Функция Заголовок(Знач ЗаголовкиЗапроса, Знач Имя) Экспорт
	Возврат Заголовки.Значение(ЗаголовкиЗапроса, Имя);
КонецФункции

&Желудь
Процедура ПриСозданииОбъекта(
	&Пластилин(Значение = "ОтветыAPI") _Ответы,
	&Пластилин(Значение = "ЗаголовкиHTTP") _Заголовки,
	&Пластилин(Значение = "ПриведениеФлагов") _Флаги = Неопределено)

	Ответы = _Ответы;
	Заголовки = _Заголовки;
	Флаги = ?(_Флаги = Неопределено, Новый ПриведениеФлагов(), _Флаги);

КонецПроцедуры
