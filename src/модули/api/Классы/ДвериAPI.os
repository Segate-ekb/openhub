// Двери API в названный пул и пакет: чтение, изменение, администрирование и пороги
// прав. Закрытая дверь неотличима от отсутствующей — обе дают 404.

Перем ДоступКПулу;   // ГвардДоступаКПулу (права) — обе двери в названный пул: чтение и изменение
Перем Права;         // СервисПрав (auth)
Перем Кабинет;       // СлужбаКабинета (кабинет)
Перем СервисПакетов; // СервисПакетов (реестр)
Перем Ответы;        // ОтветыAPI (auth)

#Область ДвериВПулИПакет

// Находит пул по имени с учётом того, читаем ли он ЭТИМ запросом: право человека и рамки
// ключа спрашиваются одной композицией — гвардом чтения пула. Названный пул — дверь, и
// ключ чужой привязки в неё не входит даже когда пул публичный. Отсутствующий, закрытый
// правами и закрытый привязкой ключа неотличимы: все три дают один и тот же 404.
// При отказе сам записывает ответ — вызывающий обязан выйти.
//
// Параметры:
//  Ответ - ОтветСервера - ответ winow.
//  ИмяПула - Строка - имя пула.
//  Принципал - Структура, Неопределено - принципал запроса; Неопределено — аноним.
//
// Возвращаемое значение:
//  Пул, Неопределено - названный пул, если он читаем этим запросом, либо Неопределено.
//
Функция ВидимыйПул(Ответ, Знач ИмяПула, Знач Принципал) Экспорт
	Возврат ПулЗаДверью(Ответ, ИмяПула, ЧитаемыйПул(ИмяПула, Принципал));
КонецФункции

// Тот же пул для запроса, пришедшего его МЕНЯТЬ. Дверь мутаций шире двери чтения ровно
// на ключ, которому в этом пуле позволено только писать: закройся она по скоупу чтения,
// такой ключ не смог бы написать и в свой собственный пул. Хватает ли скоупа на само
// действие, судит гейт мутаций (403), а не эта дверь.
//
// Параметры:
//  Ответ - ОтветСервера - ответ winow.
//  ИмяПула - Строка - имя пула.
//  Принципал - Структура, Неопределено - принципал запроса; Неопределено — аноним.
//
// Возвращаемое значение:
//  Пул, Неопределено - названный пул, если дверь изменения открыта, либо Неопределено.
//
Функция ПулМутации(Ответ, Знач ИмяПула, Знач Принципал) Экспорт
	Возврат ПулЗаДверью(Ответ, ИмяПула, ПулДляИзменения(ИмяПула, Принципал));
КонецФункции

// Единый пролог доступа для маршрутов уровня пакета: видимость пула, затем пакет.
// При отказе сам записывает 404 — вызывающий обязан выйти.
//
// Параметры:
//  Ответ - ОтветСервера - ответ winow.
//  ИмяПула - Строка - имя пула.
//  ИмяПакета - Строка - имя пакета.
//  Принципал - Структура, Неопределено - принципал запроса; Неопределено — аноним.
//
// Возвращаемое значение:
//  Структура, Неопределено - поля Пул и Пакет либо Неопределено.
//
Функция ВидимыйПакет(Ответ, Знач ИмяПула, Знач ИмяПакета, Знач Принципал) Экспорт
	Возврат ПакетЗаДверью(Ответ, ВидимыйПул(Ответ, ИмяПула, Принципал), ИмяПакета);
КонецФункции

// Тот же пролог для запроса, пришедшего пакет МЕНЯТЬ: дверь мутаций вместо двери чтения,
// дальше всё то же самое.
//
// Параметры:
//  Ответ - ОтветСервера - ответ winow.
//  ИмяПула - Строка - имя пула.
//  ИмяПакета - Строка - имя пакета.
//  Принципал - Структура, Неопределено - принципал запроса; Неопределено — аноним.
//
// Возвращаемое значение:
//  Структура, Неопределено - поля Пул и Пакет либо Неопределено.
//
Функция ПакетМутации(Ответ, Знач ИмяПула, Знач ИмяПакета, Знач Принципал) Экспорт
	Возврат ПакетЗаДверью(Ответ, ПулМутации(Ответ, ИмяПула, Принципал), ИмяПакета);
КонецФункции

// Пул административной операции: невидимый неотличим от отсутствующего (404), видимый
// без прав владельца/администратора — 403. Тот же порядок проверок, что у ГвардПула
// в /office. При отказе сам записывает ответ — вызывающий обязан выйти.
//
// Параметры:
//  Ответ - ОтветСервера - ответ winow.
//  ИмяПула - Строка - имя пула.
//  Принципал - Структура, Неопределено - принципал запроса; Неопределено — аноним.
//
// Возвращаемое значение:
//  Пул, Неопределено - администрируемый пул либо Неопределено.
//
Функция АдминистрируемыйПул(Ответ, Знач ИмяПула, Знач Принципал) Экспорт
	Возврат ПулСПравомАдмина(Ответ, ВидимыйПул(Ответ, ИмяПула, Принципал), Принципал);
КонецФункции

// Тот же администрируемый пул для запроса, пришедшего его МЕНЯТЬ: дверь мутаций, затем
// тот же порог владельца/администратора.
//
// Параметры:
//  Ответ - ОтветСервера - ответ winow.
//  ИмяПула - Строка - имя пула.
//  Принципал - Структура, Неопределено - принципал запроса; Неопределено — аноним.
//
// Возвращаемое значение:
//  Пул, Неопределено - администрируемый пул либо Неопределено.
//
Функция АдминистрируемыйПулМутации(Ответ, Знач ИмяПула, Знач Принципал) Экспорт
	Возврат ПулСПравомАдмина(Ответ, ПулМутации(Ответ, ИмяПула, Принципал), Принципал);
КонецФункции

#КонецОбласти

#Область ПорогиПрав

// Требует права управления пакетом: yank, метки, теги. Делегирует каноническому гейту
// кабинета — тому же, что и /office. При отказе сам записывает 403.
//
// Параметры:
//  Ответ - ОтветСервера - ответ winow.
//  Пользователь - Пользователь, Неопределено - субъект запроса.
//  Пул - Пул - пул пакета.
//  Пакет - Пакет - управляемый пакет.
//
// Возвращаемое значение:
//  Булево - Истина, если право есть.
//
Функция ТребоватьИзменениеПакета(Ответ, Знач Пользователь, Знач Пул, Знач Пакет) Экспорт
	Если НЕ Кабинет.МожетУправлять(Пользователь, Пул, Пакет) Тогда
		Ответы.Ошибка(Ответ, 403, "forbidden", "Недостаточно прав для изменения пакета");
		Возврат Ложь;
	КонецЕсли;
	Возврат Истина;
КонецФункции

// Требует прав владельца либо администратора пула. При отказе сам записывает 403.
//
// Параметры:
//  Ответ - ОтветСервера - ответ winow.
//  Пользователь - Пользователь, Неопределено - субъект запроса.
//  Пул - Пул - администрируемый пул.
//
// Возвращаемое значение:
//  Булево - Истина, если право есть.
//
Функция ТребоватьАдмин(Ответ, Знач Пользователь, Знач Пул) Экспорт
	Если НЕ Права.МожетАдминистрировать(Пользователь, Пул) Тогда
		Ответы.Ошибка(Ответ, 403, "forbidden",
			"Требуются права владельца/администратора пула");
		Возврат Ложь;
	КонецЕсли;
	Возврат Истина;
КонецФункции

// Требует прав администратора хаба. При отказе сам записывает 403.
//
// Параметры:
//  Ответ - ОтветСервера - ответ winow.
//  Пользователь - Пользователь, Неопределено - субъект запроса.
//
// Возвращаемое значение:
//  Булево - Истина, если право есть.
//
Функция ТребоватьАдминаХаба(Ответ, Знач Пользователь) Экспорт
	Если НЕ Права.ПользовательАдмин(Пользователь) Тогда
		Ответы.Ошибка(Ответ, 403, "forbidden", "Требуются права администратора хаба");
		Возврат Ложь;
	КонецЕсли;
	Возврат Истина;
КонецФункции

// Сервис прав для точечных проверок, которым мало готовых дверей.
//
// Возвращаемое значение:
//  СервисПрав - сервис модели прав.
//
Функция СервисПрав() Экспорт
	Возврат Права;
КонецФункции

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

// Отсутствие гварда — fail-closed: читаемых пулов у запроса тогда нет ни одного.
Функция ЧитаемыйПул(Знач ИмяПула, Знач Принципал)
	Если ДоступКПулу = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
	Возврат ДоступКПулу.ЧитаемыйПул(ПользовательЗапроса(Принципал), ИмяПула, Принципал);
КонецФункции

Функция ПулДляИзменения(Знач ИмяПула, Знач Принципал)
	Если ДоступКПулу = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
	Возврат ДоступКПулу.ПулДляИзменения(ПользовательЗапроса(Принципал), ИмяПула, Принципал);
КонецФункции

// Общий хвост обеих дверей: закрытый пул неотличим от несуществующего.
Функция ПулЗаДверью(Ответ, Знач ИмяПула, Знач Пул)
	Если Пул = Неопределено Тогда
		Ответы.Ошибка(Ответ, 404, "pool_not_found",
			СтрШаблон("Пул ""%1"" не найден", ИмяПула));
	КонецЕсли;
	Возврат Пул;
КонецФункции

// Общий хвост обоих прологов уровня пакета: пакет ищется только в пуле за дверью.
Функция ПакетЗаДверью(Ответ, Знач Пул, Знач ИмяПакета)

	Если Пул = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;

	Пакет = СервисПакетов.НайтиПакет(Пул.Идентификатор, ИмяПакета);
	Если Пакет = Неопределено ИЛИ Пакет.Скрыт = Истина Тогда
		Ответы.Ошибка(Ответ, 404, "package_not_found",
			СтрШаблон("Пакет ""%1"" не найден", ИмяПакета));
		Возврат Неопределено;
	КонецЕсли;

	Результат = Новый Структура();
	Результат.Вставить("Пул", Пул);
	Результат.Вставить("Пакет", Пакет);
	Возврат Результат;

КонецФункции

// Общий хвост обоих административных прологов: порог владельца/администратора пула.
Функция ПулСПравомАдмина(Ответ, Знач Пул, Знач Принципал)
	Если Пул = Неопределено Тогда
		Возврат Неопределено;
	КонецЕсли;
	Если НЕ ТребоватьАдмин(Ответ, ПользовательЗапроса(Принципал), Пул) Тогда
		Возврат Неопределено;
	КонецЕсли;
	Возврат Пул;
КонецФункции

Функция ПользовательЗапроса(Знач Принципал)
	Возврат ?(Принципал = Неопределено, Неопределено, Принципал.Пользователь);
КонецФункции

#КонецОбласти

&Желудь
Процедура ПриСозданииОбъекта(
	&Пластилин(Значение = "ГвардДоступаКПулу") _ДоступКПулу,
	&Пластилин(Значение = "СервисПрав") _Права,
	&Пластилин(Значение = "СлужбаКабинета") _Кабинет,
	&Пластилин(Значение = "СервисПакетов") _СервисПакетов,
	&Пластилин(Значение = "ОтветыAPI") _Ответы)

	ДоступКПулу = _ДоступКПулу;
	Права = _Права;
	Кабинет = _Кабинет;
	СервисПакетов = _СервисПакетов;
	Ответы = _Ответы;

КонецПроцедуры
