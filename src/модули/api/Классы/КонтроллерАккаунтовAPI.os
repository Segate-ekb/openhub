// POST /api/v1/users — самостоятельная регистрация пользователя с учётом онбординг-
// политики хаба. Эндпоинт неаутентифицированный, CSRF к нему не применяется.

Перем Ответы;            // ОтветыAPI (основа)
Перем Запрос;            // ЗапросAPI (api)
Перем Маршрут;           // МаршрутAPI (api)
Перем СервисРегистрации; // СервисРегистрации (auth)
Перем Отказы;            // ОтказыУчётки (auth) — код, статус и текст причины отказа

#Область ТочкиМаршрута

// POST /api/v1/users — регистрирует пользователя и отдаёт его карточку с кодом 201.
//
// Параметры:
//  Ответ - ОтветСервера - ответ winow.
//  ТелоЗапроса - Строка - JSON либо форма с полями логин, email, пароль.
//  ЗаголовкиЗапроса - Соответствие - заголовки запроса.
//  МетодЗапроса - Строка - метод запроса; допустим только POST.
//
&ТочкаМаршрута("users")
Процедура Пользователи(Ответ, ТелоЗапроса, ЗаголовкиЗапроса, МетодЗапроса) Экспорт

	Если НЕ Маршрут.НачатьДействие(Ответ, Неопределено, МетодЗапроса, "POST") Тогда
		Возврат;
	КонецЕсли;

	Тело = Запрос.РазобратьТело(ТелоЗапроса, ЗаголовкиЗапроса);
	Логин = Запрос.Поле(Тело, "логин", Запрос.Поле(Тело, "login"));
	Email = Запрос.Поле(Тело, "email");
	Пароль = Запрос.Поле(Тело, "пароль", Запрос.Поле(Тело, "password"));

	Попытка
		Пользователь = СервисРегистрации.Зарегистрировать(Логин, Email, Пароль);
	Исключение
		ОтдатьОшибкуРегистрации(Ответ, ИнформацияОбОшибке());
		Возврат;
	КонецПопытки;

	Ответы.JSON(Ответ, 201, Новый Структура("user", ПользовательВJSON(Пользователь)));

КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

// Причина отказа приезжает машинным кодом; статус, текст и подробности берёт таблица —
// одна на все двери регистрации.
Процедура ОтдатьОшибкуРегистрации(Ответ, Знач ИнфОбОшибке)
	Отказ = Отказы.Разобрать(ИнфОбОшибке);
	Ответы.Сбой(Ответ, Отказ.Статус, Отказ.Код, Отказ.Сообщение, ИнфОбОшибке, Отказ.Подробности);
КонецПроцедуры

Функция ПользовательВJSON(Знач Пользователь)
	Структура = Новый Структура();
	Структура.Вставить("id", Пользователь.Идентификатор);
	Структура.Вставить("login", Пользователь.Логин);
	Структура.Вставить("email", Пользователь.Email);
	Структура.Вставить("role", Пользователь.РольХаба);
	Возврат Структура;
КонецФункции

#КонецОбласти

&Контроллер("/api/v1")
Процедура ПриСозданииОбъекта(
	&Пластилин(Значение = "ОтветыAPI") _Ответы,
	&Пластилин(Значение = "ЗапросAPI") _Запрос,
	&Пластилин(Значение = "МаршрутAPI") _Маршрут,
	&Пластилин(Значение = "СервисРегистрации") _СервисРегистрации,
	&Пластилин(Значение = "ОтказыУчётки") _Отказы)

	Ответы = _Ответы;
	Запрос = _Запрос;
	Маршрут = _Маршрут;
	СервисРегистрации = _СервисРегистрации;
	Отказы = _Отказы;

КонецПроцедуры
