# Скрипты работают из своего каталога, поэтому всё, что они порождают, ложится сюда же.
# В индекс не берём ничего из этого: замеры и улики — данные конкретного прогона,
# а стенд.env несёт ЖИВОЙ токен доступа к хабу.
стенд.env
цель.env
замеры/
логи/
улики/
итоги/
пакеты/
маршруты.txt
режим-ступени.yaml
