# Контекст сборки образа: только пакет, packagedef и распаковщик (см. Dockerfile).
# Всё остальное — включая oscript_modules на 138 МБ — в образ едет ВНУТРИ .ospx.
*
!openhub-*.ospx
!packagedef
!scripts/РаспаковатьПакет.os
