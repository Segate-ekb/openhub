// Прогревщик README, всегда роняющий прогрев — для проверки, что сбой фоновой рогатки
// доходит до журнала.

// Контракт ПрогревщикREADME; всегда бросает исключение.
//
Процедура ПрогретьВсе() Экспорт
	ВызватьИсключение "ПрогревщикПадающий: намеренный сбой прогрева";
КонецПроцедуры
