// Заглушка словаря текстов: позволяет положить под ключом пустое значение.

Перем Тексты;

// Кладёт текст под ключом, в том числе пустой.
//
// Параметры:
//  Ключ - Строка - ключ текста.
//  Текст - Строка - значение; допускается пустое.
//
Процедура Положить(Знач Ключ, Знач Текст) Экспорт
	Тексты.Вставить(Ключ, Текст);
КонецПроцедуры

// Контракт СловарьТекстов: текст по ключу.
//
// Параметры:
//  Ключ - Строка - ключ текста.
//
// Возвращаемое значение:
//  Строка - текст либо Неопределено, если ключа нет.
//
Функция НайтиТекст(Знач Ключ) Экспорт
	Возврат Тексты.Получить(Ключ);
КонецФункции

// Все ключи словаря.
//
// Возвращаемое значение:
//  Массив из Строка.
//
Функция Ключи() Экспорт
	Результат = Новый Массив();
	Для Каждого КиЗ Из Тексты Цикл
		Результат.Добавить(КиЗ.Ключ);
	КонецЦикла;
	Возврат Результат;
КонецФункции

Процедура ПриСозданииОбъекта()
	Тексты = Новый Соответствие();
КонецПроцедуры
