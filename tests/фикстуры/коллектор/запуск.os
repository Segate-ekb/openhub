// Точка входа фейкового OTLP-коллектора: winow с единственным контроллером-приёмником.
// Порт и путь дампов приходят окружением FAKE_OTLP_*.

#Использовать autumn
#Использовать autumn-logos
#Использовать winow

КаталогЗапуска = Новый Файл(ТекущийСценарий().Источник).Путь;

Детальки = Новый Соответствие();
Детальки.Вставить("winow.АвтоСтарт", Истина);
Детальки.Вставить("winow.ИспользоватьПрикладнойСервер", Ложь);
Детальки.Вставить("winow.КаталогСПриложениями", ОбъединитьПути(КаталогЗапуска, "Классы"));
Детальки.Вставить("winow.Порт", Число(ПолучитьПеременнуюСреды("FAKE_OTLP_PORT")));

Совет = Новый СоветДругогоМастера();
Совет.ЗначенияДеталек(Детальки);

Поделка = Новый Поделка(Совет);
Поделка.ЗапуститьПриложение();
